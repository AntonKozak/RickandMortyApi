class Char {
    // Get info which we need using Class
    constructor(id, name, image, status){
        this.id = id;
        this.name = name;
        this.image = image;
        this.status = status;
    }
}

export default Char;